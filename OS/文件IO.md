
> 对于内核而言,所有打开的文件都通过文件描述符引用。文件描述符是一个非负整数。
  当打开一个现有文件或创建一个新文件时, **内核向进程返回一个文件描述符**。
  当读、写一个文件时, *1、使用 open 或 creat 返回的文件描述符标识该文件, 2、将其作为参数传送给read 或write*
> 按照惯例,UNIX系统 shell 把 **文件描述符 0 与 进程的标准输入** 关联, **文件描述符 1 与 标准输出** 关联，**文件描述符 2 与 标准错误** 相关联

### open 和 openat 函数 
